import React, { Component } from 'react';
import Terminal from '../snippets/terminal.snippet';

class Cmp extends Component {
  render() {
    return (
      <div>
        <Terminal></Terminal>
      </div>
    );
  }
}

export default Cmp;
